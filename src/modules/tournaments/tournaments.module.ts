import { Module } from '@nestjs/common';

/**
 * Roadmap. Bracket and swiss formats over the existing race machinery.

 * Endpoints: GET /tournaments · POST /tournaments/:id/register
 *
 * Not implemented yet — registered so the module graph matches the documented
 * architecture from the first commit.
 */
@Module({})
export class TournamentsModule {}
