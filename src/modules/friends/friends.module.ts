import { Module } from '@nestjs/common';

/**
 * Invites, accepts, and the friends-scoped leaderboard's membership source.

 * Endpoints: GET /friends · POST /friends/invite · POST /friends/:id/accept
 *
 * Not implemented yet — registered so the module graph matches the documented
 * architecture from the first commit.
 */
@Module({})
export class FriendsModule {}
